---
type: 代码实现
---
# 1. 实现分析

在上一节我们已经实现了AI创建待办的功能，后续实现AI审批的话，方案上跟前面是类似的。只是实现上有一些细微的差异

从前面主体业务功能实现审批内容中来看，审批添加的情况有很多种【请假、补卡…】并且每个类型的数据字段是不一样的。**按照类似于待办的处理方式，** 如果直接用outputparser所有类型都写上的话，会很复杂，并且每次添加一个新的审批类型都需要去修改提示词，很显然这种实现会非常复杂。

![](<images/8. AIChat处理审批业务-image.png>)

从上图分析可知，审批操作的类型其实是很多的，每种类型都会有对应类型的字段需求，而且每次用户他只会操作一种类型，所以这个时候如果我们用常规的方式将所有类型的字段。都放到大模型里面去，由大模型根据用户的信息，然后结合所有字段去生成，那么容易会造成结果性的误差，会提高程序的一个复杂度以及token消耗。

所以我们需要采用单一设计去实现。先用模型去判断当前的操作是什么类型。然后我们再基于审批类型，

![](<images/8. AIChat处理审批业务-1.png>)

在AI的对接交互中，我们首先获取到审批的类型，审批类型我们可以通过用户所输入的信息是能够验证出来的，我们会根据用户所说的信息，然后去验证当前用户所要操作的审批类型了。得到审批类型之后，我们就可以由对应审批类型的对象去完成审批创建的动作了

# 2. 构建审批业务对象

在这个toolx这个包下面构建审批对象，在toolx包下创建approval目录，。当前这个approval包，是属于整个审批业务的，在approval包下的approval.go文件中实现审批接口，具体的代码较多，这里就不做过多展示，查看源码即可

在审批动作接口完成之后，还需要完成审批添加的工作，这里需要根据用户的输入信息判断审批类型，然后完成审批添加，这里我们直接在toolx包下新建一个approvaladd.go文件实现

```go
package toolx

import (
    "AIWorkHelper/internal/logic/chatinternal/toolx/approval"
    "AIWorkHelper/internal/model"
    "AIWorkHelper/internal/svc"
    "AIWorkHelper/pkg/langchain/outputparserx"
    "context"
    "github.com/tmc/langchaingo/callbacks"
)

type ApprovalAdd struct {
    svc          *svc.ServiceContext
    Callback     callbacks.Handler
    outputparser outputparserx.Structured
}

func NewApprovalAdd(svc *svc.ServiceContext) *ApprovalAdd {
    return &ApprovalAdd{
       svc:      svc,
       Callback: svc.Callbacks,
       outputparser: outputparserx.NewStructured([]outputparserx.ResponseSchema{
          {
             Name: "type",
             Description: "approval type; enum : 1. General\n 2. Leave Approval\n 3. " +
                "Card replacement Approval\n 4. Go out Approval",
             Type: "int",
          }, {
             Name:        "input",
             Description: "The user's original input",
          },
       }),
    }
}

func (a *ApprovalAdd) Name() string {
    return "approval_add"
}

func (a *ApprovalAdd) Description() string {
    return `
    a approval add interface
    use when you need to create a approval.
    keep Chinese output.
` + a.outputparser.GetFormatInstructions()
}

func (a *ApprovalAdd) Call(ctx context.Context, input string) (string, error) {
    if a.Callback != nil {
       a.Callback.HandleText(ctx, "approval add start input : "+input)
    }

    out, err := a.outputparser.Parse(input)
    if err != nil {
       return "", err
    }
    data := out.(map[string]any)

    var approvalType float64
    if t, ok := data["type"]; ok {
       approvalType = t.(float64)
    }
    if v, ok := data["input"]; ok {
       input = v.(string)
    }

    input = "The company's working hours are normal working hours of 8 hours a day and 40 hours a week; Monday to Friday 9:30-11:30 13:00-18:00"

    ap, err := approval.NewApproval(a.svc, model.ApprovalType(approvalType))
    if err != nil {
       return "", err
    }

    id, err := ap.Create(ctx, input)
    if err != nil {
       return "", err
    }

    return Success + "\n created approval id : " + id, nil
}
```

# 3. 完善AI助手审批流程

## 3.1 审批查询

审批查询功能和审批创建的实现很像，也是在toolx包下直接创建一个approvalfind.go文件来实现

```go
package toolx

import (
    "AIWorkHelper/internal/domain"
    "AIWorkHelper/internal/svc"
    "AIWorkHelper/pkg/curl"
    "AIWorkHelper/pkg/langchain/outputparserx"
    "AIWorkHelper/pkg/token"
    "context"
    "github.com/tmc/langchaingo/callbacks"
)

type ApprovalFind struct {
    svc          *svc.ServiceContext
    Callback     callbacks.Handler
    outputparser outputparserx.Structured
}

func NewApprovalFind(svc *svc.ServiceContext) *ApprovalFind {
    return &ApprovalFind{
       svc:      svc,
       Callback: svc.Callbacks,
       outputparser: outputparserx.NewStructured([]outputparserx.ResponseSchema{
          {
             Name:        "type",
             Description: "approval type; enum : 0. General, 1. Leave, 2. Card replacement, 3. Go out, 4. Reimbursement, 5. Payment, 6. Purchase, 7. Collection; number to be completed",
             Type:        "int",
          }, {
             Name:        "id",
             Description: "approval id",
          }, {
             Name:        "status",
             Description: "approval status; enum : 0. No beginning, 1. In progress 2. Done-Passed, 3. Revocation, 4. refused; number to be completed",
          }, {
             Name:        "createId",
             Description: "id of creator",
          },
       }),
    }
}

func (a *ApprovalFind) Name() string {
    return "approval_find"
}

func (a *ApprovalFind) Description() string {
    return `
    a approval find interface.
    use when you need to find a approval.
    If the condition is null, return {}
    keep Chinese output.
` + a.outputparser.GetFormatInstructions()
}

func (a *ApprovalFind) Call(ctx context.Context, input string) (string, error) {
    if a.Callback != nil {
       a.Callback.HandleText(ctx, "approval find start input : "+input)
    }

    out, err := a.outputparser.Parse(input)
    if err != nil {
       return "", err
    }

    data := out.(map[string]any)
    if data == nil {
       data = make(map[string]any)
    }
    if data["createId"] == nil {
       data["createId"] = token.GetUId(ctx)
    }

    res, err := curl.GetRequest(token.GetTokenStr(ctx), a.svc.Config.Host+"/v1/approval/list", data)
    if err != nil {
       return "", err
    }

    if a.Callback != nil {
       a.Callback.HandleText(ctx, "approval find end data : "+string(res))
    }

    return ResParser(res, domain.ApprovalFind, nil)
}
```

## 3.2 审批handler

在完成好审批查询之后，就可以实现审批处理对象handler了。在/logic/logic/chatinternal包下直接创建一个approvalhandler.go文件来实现

```go
package chatinternal

import (
    "AIWorkHelper/internal/logic/chatinternal/toolx"
    "AIWorkHelper/internal/svc"
    "github.com/tmc/langchaingo/tools"
)

type ApprovalHandle struct {
    *baseChat
}

func NewApprovalHandle(svc *svc.ServiceContext) *ApprovalHandle {
    return &ApprovalHandle{
       baseChat: NewBaseChat(svc, []tools.Tool{
          toolx.NewApprovalAdd(svc),
          toolx.NewApprovalFind(svc),
       }),
    }
}
func (t *ApprovalHandle) Name() string {
    return "approval"
}

func (t *ApprovalHandle) Description() string {
    return "This is about approval matters. Such as sick leave, personal leave, going out, etc.\n\n"
}
```

在完成好approvehandler之后，需要把对应handler加到chat对象中，在/logic/chat.go文件中的NewChat函数中加入approvehandler对象

```go
// NewChat 创建聊天业务逻辑实例
func NewChat(svc *svc.ServiceContext) Chat {
    // 初始化处理器列表，包含待办事项处理器
    handlers := []router.Handler{
       chatinternal.NewTodoHandle(svc), // 待办事项处理器
       chatinternal.NewKnowledge(svc),  // 知识库处理器
       chatinternal.NewApprovalHandle(svc), // 审批处理器
    }
    // 创建多会话内存管理器，支持对话历史记忆
    memory := memoryx.NewMemoryx(func() schema.Memory {
       return memoryx.NewSummaryBuffer(svc.LLMs, 50, memoryx.WithCallback(svc.Callbacks),
          memoryx.WithOutParser(memoryOutput))
    })
    return &chat{
       memory: memory,
       svc:    svc,
       router: router.NewRouter(svc.LLMs, handlers,
          router.WithMemory(memory),          // 添加记忆组件支持
          router.Withcallback(svc.Callbacks), // 添加回调处理器
          router.WithEmptyHandler(chatinternal.NewDefaultHandler(svc)),
       ), // 添加内存组件支持
    }
}
```

## 3.3 审批功能单测

审批的业务流程还是比较复杂的，而且审批的类型也很多，我们可以为审批的每个环节写一个单测，这样有效保证我们的开发的功能正确性。的在目前我们的流程中，用户输入一个请求信之后，通过chart这个业务chart，然后基于RouterChain去进行业务的一个转发。它就会把用户原始的一个出信息发送到审批的这个handler中。然后审批handler会根据agent去选择创建还是查询。

![](<images/8. AIChat处理审批业务-3.png>)

单测实现我们主要在/logic/chatinternal目录下新建approvalhandle\_test.go文件和chatinternal\_test.go文件来实现，这里我们看下approvalhandle\_test.go的单测实现

```go
package chatinternal

import (
    "AIWorkHelper/pkg/langchain"
    "AIWorkHelper/pkg/token"
    "context"
    "testing"

    "github.com/tmc/langchaingo/chains"
)

func TestApproval(t *testing.T) {
    // 注意这里的token要换成root用户登陆的token
    tokenStr := "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJhaXdvcmtoZWxwZXIiOiI2OGY0OTJjMGY5ODk1NjFlZGU5MzIxYzkiLCJleHAiOjE3Njk0OTg4MjgsImlhdCI6MTc2MDg1ODgyOH0.lUPaCOMPQNPKDT2c0GGA0mlYn1q2aCnEYDvkp9roLds"

    ctx := context.Background()
    ctx = context.WithValue(ctx, token.Authorization, tokenStr)

    chat := NewApprovalHandle(svcTest)
    res, err := chat.baseChat.transform(ctx, map[string]any{
       langchain.Input: "提交一个明天上午请假审批",
       "history":       "",
    }, chains.WithCallback(svcTest.Callbacks))

    if err != nil {
       t.Fatal(err)
    }

    t.Log(res)
}
```

然后我们启动服务，先登录用户，把登录获取到的token替换上述代码的tokenStr，然后就可以测试AI创建审批的功能了。正常情况下的测试结果如下图所示

![](<images/8. AIChat处理审批业务-image-1.png>)

这里我们再查看一下数据库可以看到审批数据已经创建成功

![](<images/8. AIChat处理审批业务-image-2.png>)

### 3.3.1 前端测试

创建补卡审批单

![](<images/8. AIChat处理审批业务-image-3.png>)

![](<images/8. AIChat处理审批业务-image-4.png>)

查询审批单

![](<images/8. AIChat处理审批业务-image-5.png>)

审批单查询：

![](<images/8. AIChat处理审批业务-image-6.png>)

AI外出审批样例，可以测试以下场景：

1. 具体时间:

2025-10-24 09:00 到 11:00 我要拜访客户

2. 相对时间:

明天上午9点到11点我要外出办事

3. 中文时间格式:

10月24日9点到11点我要见客户

