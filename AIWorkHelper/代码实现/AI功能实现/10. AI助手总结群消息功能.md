---
type: 代码实现
---
# 1. 功能分析

关于群聊我们的主要流程是：

1. 获取群聊消息内容

2. 调用大模型总结

3. 将总结结果返回给用户

4. 用户根据总结结果创建待办或审批，因为有一些功能是在群聊天中就确定了

![](<images/10. AI助手总结群消息功能-4.png>)

这里有一个问题，为什么不直接根据总结结果由AI完成待办呢？不这么做的关键因素有两个

> 1. 大模型总结不一定符合用户需求
>
> 2. 大模型存在一定的不稳定性

对话中有多种聊天情况怎么处理。对于消息的总结一般可能同时有多种情况的产生，有的内容适合去创建待办任务，有的则是属于请假，当然也有单纯只聊天的。

因此我们可以让大模型根据如下格式返回，那么这样的话就可以根据操作类型进行相应的处理。

```go
[
  {
    type: int,        // task typ; enum : 1. task to be done, 2 approval
    title: string,    // title
    content: string   // content
  }, {
    type: int,        // task typ; enum : 1. task to be done, 2 approval
    title: string,    // title
    content: string   // content
  }
]
```

因此提示词可以这样写

```go
Please summarize based on the following chat conversations
- Role: You are an office assistant who helps employees summarize communication matters at work. Matters mainly include things such as “tasks to be done, leave for leave” work‘
1. You must first read the chat content to understand the relationship between employees and distinguish between superiors and subordinates
2. You need to distinguish what matters are based on the chat content, such as [tasks to be done, leave for leave]
4. You need to first obtain the overall context of the matter, and then summarize it based on the context, such as: the person initiated the leave, whether there are any follow-up, etc.
5. Personnel information needs to be fully output

- chatlog
{{.input}}


- require
The output content needs to be output in the following format and can be parsed by json
[
  {
    type: int,        // task typ; enum : 1. task to be done, 2 approval
    title: string,    // title
    content: string   // content
  }, {
    type: int,        // task typ; enum : 1. task to be done, 2 approval
    title: string,    // title
    content: string   // content
  }
]

```

写完后用AI生成一段聊天的对话，将对话与我们的提示词组合向AI提问看是否能够总结出相应的事项，完整实例

```go
Please summarize based on the following chat conversations
- Role: You are an office assistant who helps employees summarize communication matters at work. Matters mainly include things such as “tasks to be done, leave for leave” work‘
1. You must first read the chat content to understand the relationship between employees and distinguish between superiors and subordinates
2. You need to distinguish what matters are based on the chat content, such as [tasks to be done, leave for leave]
4. You need to first obtain the overall context of the matter, and then summarize it based on the context, such as: the person initiated the leave, whether there are any follow-up, etc.
5. Personnel information needs to be fully output

- chatlog
山药：我们今天主要讨论一下项目的进展情况，以及接下来要解决的几个问题。木兮，你先说说后端的进展吧。  
木兮：好的。山药，后端这边的功能开发大致完成了，现在正在进行一些接口的调试和优化。预计这周三能完成所有接口的开发和测试。  
山药：很好。但你们的接口文档是否更新好了？上次会议上提到的文档问题解决了吗？  
木兮：我已经更新了，昨天发给了大家，请大家尽快查看，特别是小王。你负责测试，能否在周四之前给出反馈？  
小王：没问题，我会尽快在测。之前我在测试中也发现了一些小问题，这两天会尽量整理出来。  
山药：小王，关于你之前提到的那几个问题，能否明天下午之前把整理好的列表发给大家？  
小王：可以的，我会争取在明天下午三点之前发给你们。  
山药：好的。事情紧，前端这边有什么进展？  
小李：前端的功能模块也差不多完成，现在正在进行组件的优化。原定周五完成，但由于有几处细节需要改动，可能会延到下周一。  
山药：下周一必须完成。如果时间紧，是否需要增加人手协助？  
小李：如果能再协调到一个同事帮忙的话，进度会快很多。  
山药：那我联系一下其他部门，看能否帮调一位同事。还有你提到的请假问题，小露，你大概什么时候请假？  
小露：我打算下周四请一天假，去参加一个前端开发的研讨会。会议的内容会对我们目前的项目有所帮助。  
山药：好的。尽量提前请假的工作手续，不要影响项目进度。木兮，你这边在能否在周四之前把相关接口梳理清楚，方便小露的接入。  
木兮：没问题，我尽快处理。  
山药：很好，今天的讨论到此为止。周四我们再作个会议检查进展情况。大家有什么问题随时沟通，确保按时完成客户的工作。

- require
The output content needs to be output in the following format and can be parsed by json
[
  {
    type: int,        // task typ; enum : 1. task to be done, 2 approval
    title: string,    // title
    content: string   // content
  }, {
    type: int,        // task typ; enum : 1. task to be done, 2 approval
    title: string,    // title
    content: string   // content
  }
]
```

简单验证下上述提示词的正确性，将上述提示词发送给chatGPT可以得到如下回复

```json
[
  {
    "type": 1,
    "title": "后端接口开发与文档更新任务",
    "content": "木兮负责的后端功能开发基本完成，正在进行接口调试和优化，预计周三完成所有接口开发与测试。接口文档已更新并发送给大家，需小王在周四前反馈测试结果。"
  },
  {
    "type": 1,
    "title": "测试问题整理与反馈任务",
    "content": "小王需在明天下午三点前整理并发送测试中发现的问题列表，以便团队及时处理。"
  },
  {
    "type": 1,
    "title": "前端开发进度与优化任务",
    "content": "小李负责的前端模块基本完成，正在进行组件优化。原计划周五完成，但因细节改动可能延至下周一。若能协调一名同事协助，可加快进度。"
  },
  {
    "type": 1,
    "title": "协调跨部门人手支持任务",
    "content": "山药将联系其他部门协调一名同事协助前端优化，以确保项目进度不受影响。"
  },
  {
    "type": 2,
    "title": "请假审批——小露",
    "content": "小露计划下周四请一天假，参加一个前端开发研讨会。山药同意其请假，但要求提前办理手续并确保不影响项目进度。"
  },
  {
    "type": 1,
    "title": "接口梳理任务以支持小露的接入",
    "content": "木兮需在周四前完成相关接口的梳理工作，以方便小露后续接入项目。"
  },
  {
    "type": 1,
    "title": "项目进度会议安排",
    "content": "山药安排在周四召开会议，检查项目整体进展，确保项目按时完成。"
  }
]

```

`type: 1` 表示待办任务（tasks to be done）

`type: 2` 表示审批或请假事项（approval/leave）

说明提示词的总结功能是没有问题的，总结内容提取了每个明确的任务或请假事项，并保留了关键时间、负责人和目标。

# 2. 功能实现

## 2.1 会话汇总

根据流程AI会基于我们传递的关联id从聊天记录中获取到对应的消息总结，生成会话对话的文本信息；我们可以基于如下方式展示

```plain&#x20;text
张经理：我们今天主要讨论一下项目的进展情况，以及接下来要解决的几个问题。王员工，你先说说后端的进展吧。
王员工：好的。张经理，后端这边的功能开发大致完成了，现在正在进行一些接口的调试和优化。预计这周三能完成所有接口的开发和测试。  
张经理：很好。但你们的接口文档是否更新好了？上次会议上提到的文档问题解决了吗？  
王员工：我已经更新了，昨天发给了大家，请大家尽快查看，特别是李员工。你负责测试，能否在周四之前给出反馈？
李员工：没问题，我会尽快在测。之前我在测试中也发现了一些小问题，这两天会尽量整理出来。
```

所以，需要首先添加聊天对话根据根据时间查询的方法，在/internal/model/chatmodel.go中完成如下方法添加，根据会话id和开始结束时间完成会会话的汇总

```go
func (m *defaultChatLogModel) ListBySendTime(ctx context.Context, cid string, sendStartTime, sendEndTime int64) ([]*ChatLog, error) {
    var data []*ChatLog

    opt := &options.FindOptions{
       Sort: bson.M{
          "sendTime": 1,
       },
    }

    // 定义查询字段，根据会话id进行查询
    filter := bson.M{
       "conversationId": cid,
    }

    if sendStartTime > 0 {
       filter["sendTime"] = bson.M{
          "$gte": sendStartTime,
       }
    }

    if sendEndTime > 0 {
       filter["sendTime"] = bson.M{
          "$lte": sendEndTime,
       }
    }

    err := entityList(ctx, m.col, filter, &data, opt)

    return data, err
}
```

## 2.2 会话总结

会话的总结功能需要用到大模型，处理的是聊天信息，这里我们同意在/internal/chatinternal包下实现，在该目录下创建chatloghandle.go文件完成会话总结功能的实现

```go
package chatinternal

import (
    "AIWorkHelper/internal/domain"
    "AIWorkHelper/internal/model"
    "AIWorkHelper/internal/svc"
    "AIWorkHelper/pkg/langchain"
    "AIWorkHelper/pkg/langchain/outputparserx"
    "context"
    "encoding/json"
    "errors"
    "fmt"
    "github.com/tmc/langchaingo/chains"
    "github.com/tmc/langchaingo/prompts"
    "strings"
    "time"
)

type Task struct {
    Type    int
    Title   string
    Content string
}

type ChatLogHandle struct {
    svc    *svc.ServiceContext
    chains chains.Chain
    out    outputparserx.Structured
}

func NewChatLogHandle(svc *svc.ServiceContext) *ChatLogHandle {
    return &ChatLogHandle{
       svc:    svc,
       chains: chains.NewLLMChain(svc.LLMs, prompts.NewPromptTemplate(_defaultChatLogPrompts, []string{"input"})),
       out:    outputparserx.Structured{},
    }
}

func (c *ChatLogHandle) Name() string {
    return "chat_log"
}

func (c *ChatLogHandle) Description() string {
    return "used to summarize and analyze the content of a chat session"

}

func (c *ChatLogHandle) Chains() chains.Chain {
    return chains.NewTransform(c.transform, nil, nil)
}

func (c *ChatLogHandle) transform(ctx context.Context, inputs map[string]any,
    opts ...chains.ChainCallOption) (map[string]any, error) {
    var cid string
    if id, ok := inputs["relationId"].(string); !ok {
       return nil, errors.New("请确定需要总结的会话对象")
    } else {
       cid = id
    }

    startTime, endTime := setTimeRange(inputs)

    msgs, err := c.chatLog(ctx, cid, startTime, endTime)
    if err != nil {
       return nil, err
    }

    // 总结
    res, err := chains.Call(ctx, c.chains, map[string]any{
       "input": msgs,
    })
    if err != nil {
       return nil, err
    }

    text, ok := res[langchain.OutPut].(string)
    if !ok {
       return nil, chains.ErrInvalidOutputValues
    }

    var data []*Task
    if err := json.Unmarshal([]byte(text), &data); err != nil {
       return nil, err
    }

    b, err := json.Marshal(domain.ChatResp{
       ChatType: domain.ChatLog,
       Data:     data,
    })
    if err != nil {
       return nil, err
    }

    return map[string]any{
       langchain.OutPut: string(b),
    }, nil
}

func (c *ChatLogHandle) chatLog(ctx context.Context, cid string, startTime, endTime int64) (string, error) {
    list, err := c.svc.ChatLogModel.ListBySendTime(ctx, cid, startTime, endTime)
    if err != nil {
       return "", err
    }

    // todo
    chatStr := "%s(%s): %s\n"

    var (
       res    strings.Builder
       record = make(map[string]*model.User)
    )

    for i, _ := range list {
       var u *model.User
       if v, ok := record[list[i].SendId]; ok {
          u = v
       } else {
          t, err := c.svc.UserModel.FindOne(ctx, list[i].SendId)
          if err != nil {
             return "", err
          }

          u = t
          record[list[i].SendId] = t
       }

       res.Write([]byte(fmt.Sprintf(chatStr, u.Name, u.ID.Hex(), list[i].MsgContent)))
    }

    return res.String(), nil
}

func setTimeRange(input map[string]any) (int64, int64) {
    var startTime, endTime int64

    cuurentTime := time.Now().Unix()
    if start, ok := input["startTime"].(int); ok && start > 0 {
       startTime = int64(start)
    } else {
       // 获取前一天的消息
       startTime = cuurentTime - 24*3600
    }

    if end, ok := input["endTime"].(int); ok && end > 0 {
       endTime = int64(end)
    } else {
       // 获取前一天的消息
       endTime = cuurentTime
    }

    return startTime, endTime
}
```

# 3. 功能测试

聊天内容

```plain&#x20;text
张经理：我们今天主要讨论一下项目的进展情况，以及接下来要解决的几个问题。王员工，你先说说后端的进展吧。
王员工：好的。张经理，后端这边的功能开发大致完成了，现在正在进行一些接口的调试和优化。预计这周三能完成所有接口的开发和测试。  
张经理：很好。但你们的接口文档是否更新好了？上次会议上提到的文档问题解决了吗？  
王员工：我已经更新了，昨天发给了大家，请大家尽快查看，特别是李员工。你负责测试，能否在周四之前给出反馈？
李员工：没问题，我会尽快在测。之前我在测试中也发现了一些小问题，这两天会尽量整理出来。
```

张经理聊天界面

![](<images/10. AI助手总结群消息功能-image.png>)

张经理聊天界面

![](<images/10. AI助手总结群消息功能-image-1.png>)

王员工聊天界面

![](<images/10. AI助手总结群消息功能-image-2.png>)

李员工聊天界面

![](<images/10. AI助手总结群消息功能-image-3.png>)





