一个健壮的后端服务，离不开完善的日志系统和优雅的错误处理机制。这能帮助我们在系统出问题时快速定位、解决问题。在项目中，错误新信息大致可以分为两类：

- **系统异常**: 比如数据库连接失败、代码里的空指针等。这类异常是程序内部的错误，不应该把原始的、充满技术细节的错误信息（比如一长串的堆栈跟踪）直接暴露给前端用户，这既不友好也不安全。我们应该记录详细的日志供开发者排查，但只返回给用户一个通用的错误提示，如“服务器开小差了”。
- **用户异常（业务异常）**: 比如用户注册时用户名重复、登录时密码错误等。这类异常错误是由于用户的操作不符合业务规则导致的，我们需要明确地告诉用户错在哪里，比如“用户名已存在”或“密码错误”。

# 1. 错误信息处理

## 1.1 Error封装实现

为了实现上述目标，我们可以封装一个自定义的错误类型。在 `pkg`目录下封装一个`xerr` 包，创建xerr.go文件完成对异常error的封装。封装之后的xerr.go文件内容如下，注释写的很详细，这里就不多做过多介绍了

```go
// Package xerr 提供了增强的错误处理功能，支持错误包装和调用栈信息追踪
package xerr

import (
    "fmt"
    "github.com/pkg/errors"
    "runtime"
    "strconv"
)

// callerSkipOffset 定义了获取调用者信息时需要跳过的栈帧数量
// 用于准确定位到实际调用错误处理函数的代码位置
const callerSkipOffset = 3

// withMessage 是一个包装错误的结构体，包含原始错误和附加消息
type withMessage struct {
    cause error  // 原始错误
    msg   string // 附加的错误消息，比如我们自己添加的记录
}


//  New 创建一个包含文件路径、行号和错误信息的新错误
func New(err error) error {
    var path string
    f, ok := getCallerFrame(0)
    if ok {
       path = f.File + ":" + strconv.Itoa(f.Line) + ":" + err.Error()
    }
    return errors.WithMessage(err, path)
}

// WithMessage 创建一个包含文件路径、行号和并且包含我们自定义的附加信息的错误
func WithMessage(err error, message string) error {
    if err == nil {
       return nil
    }
    var path string
    f, ok := getCallerFrame(0)
    if ok {
       path = f.File + ":" + strconv.Itoa(f.Line)
    }
    return &withMessage{
       cause: err,
       msg:   path + ":" + message,
    }
}

// WithMessage 创建一个包含文件路径、行号和并且包含我们自定义的格式化附加信息的错误
func WithMessagef(err error, format string, v ...any) error {
    if err == nil {
       return nil
    }
    var path string
    f, ok := getCallerFrame(0)
    if ok {
       path = f.File + ":" + strconv.Itoa(f.Line)
    }
    return &withMessage{
       cause: err,
       msg:   path + ":" + fmt.Sprintf(format, v...),
    }
}


//   返回错误里的自定义的附加信息
func (w *withMessage) Error() string {
    return w.msg
}

//  返回原始错误信息
func (w *withMessage) Cause() error {
    return w.cause
}

// getCallerFrame 获取调用者的栈帧信息，包括文件路径和行号
func getCallerFrame(skip int) (frame runtime.Frame, ok bool) {
    pc := make([]uintptr, 1)
    numFrames := runtime.Callers(skip+callerSkipOffset, pc)
    if numFrames < 1 {
       return
    }

    frame, _ = runtime.CallersFrames(pc).Next()
    return frame, frame.PC != 0
}
```

这里解释一下为什么要设置`callerSkipOffset = 3`。

这个值的设置与 Go 语言的调用栈机制有关。当我们调用 `runtime.Callers()` 时，它会返回调用栈的信息。让我们看看调用链。当我们调用 `WithMessage` 或其他函数时，调用栈通常是这样的：

> 栈帧 0: `runtime.Callers()` 本身\
> 栈帧 1: `getCallerFrame()` 函数\
> 栈帧 2: `WithMessage()` 或 `WithMessagef()` 或 `New()` 函数\
> 栈帧 3: 实际调用错误处理函数的用户代码 ← 这是我们想要的位

所以，这里跳过3，就正好能到我们业务代码的位置

在完成了error的封装之后，我们可以在`xerr`包下再新建一个xerr_test.go来测试下

```go
package xerr

import (
    "errors"
    "testing"
)

func TestWithMessage(t *testing.T) {
    err := WithMessage(errors.New("测试"), "测试输出")
    t.Log(err)

    type cause interface {
       Cause() error
    }

    e, ok := err.(cause)
    if ok {
       t.Log(e.Cause().Error())
    }
}
```

执行这个测试函数，程序输出

![BlockNote image](https://ls8sck0zrg.feishu.cn/space/api/box/stream/download/asynccode/?code=ZTY5ZGI1N2FmMDIwYTA0MWRmMzYyMGJiYTY0YzI2MjlfM0FYUGdTS3F1amRsbUJqZThoZHhKZENFWXowSFdqVzZfVG9rZW46VFBPa2JvZlN1b1d5Y0d4NkZwNWNrZGNTblNjXzE3Nzc4Mzc0OTI6MTc3Nzg0MTA5Ml9WNA)

可以看到跟预期效果一致，cause会打印出原始错误，WithMessage创建出来的错误会包含我们自定义的错误附信息以及错误发生的代码位置

## 1.2 封装后的Error使用

在完成了对error的封装之后，我们就可以在项目中来使用了，这里简单列举几个地方作为样例使用下。

1. 首先我们可以在`/internal/model/usermodel.go`中的`FindOne`方法中来使用，在`primitive.ObjectIDFromHex(id)`调用出错之后，不仅仅返回ErrInvalidObjectId，还将对应的id也一起包装到error里返回。方便调试

```go
func (m *defaultUserModel) FindOne(ctx context.Context, id string) (*User, error) {
    oid, err := primitive.ObjectIDFromHex(id)
    if err != nil {
       //return nil, ErrInvalidObjectId
       return  nil,xerr.WithMessagef(ErrInvalidObjectId,"id: %v",id)
    }

    var data User
    err = m.col.FindOne(ctx, bson.M{"_id": oid}).Decode(&data)
    switch err {
    case nil:
       return &data, nil
    case mongo.ErrNoDocuments:
       return nil, ErrNotUser
    default:
       return nil, err
    }
}
```

- 同样在`/internal/logic/user.go`文件`Login`函数中，如果登陆失败，我们可以在错误信息中打印出登陆的用户名

```go
func (l *user) Login(ctx context.Context, req *domain.LoginReq) (resp *domain.LoginResp, err error) {
    // 根据用户名查找用户
    user, err := l.svcCtx.UserModel.FindByName(ctx, req.Name)
    if err != nil {
       return nil, err
    }

    // 验证密码是否正确
    if !encrypt.ValidatePasswordHash(req.Password, user.Password) {
       return nil, errors.New("密码错误")
    }

    // 生成 JWT Token
    now := time.Now().Unix()
    token, err := token.GetJwtToken(l.svcCtx.Config.Jwt.Secret, now, l.svcCtx.Config.Jwt.Expire, user.ID.Hex())
    if err != nil {
       return nil, xerr.WithMessagef(err, "Login|GetJwtToken err with name: %s", req.Name)
    }

    // 返回登录成功响应
    return &domain.LoginResp{
       Id:           user.ID.Hex(),                    // 用户ID
       Name:         user.Name,                        // 用户名
       AccessToken:  token,                            // JWT Token
       AccessExpire: l.svcCtx.Config.Jwt.Expire + now, // Token 过期时间
    }, nil
}
```

# 2. 日志处理

日志处理项目中我们选用[https://gitee.com/dn-jinmin/tlog这个库来实现，主要解决的是**分布式链路追踪问题，**在每个请求进入系统时，为它生成一个全局唯一的ID（通常称为](https://gitee.com/dn-jinmin/tlog这个库来实现，主要解决的是**分布式链路追踪问题，**\在每个请求进入系统时，为它生成一个全局唯一的ID（通常称为) `TraceID` 或 `RequestID`），并将这个ID贯穿整个请求处理的生命周期，在打印每一条日志时都带上它。这样，我们就可以通过筛选同一个 `TraceID`，轻松地还原出一次完整请求的所有日志。

## 2.1 配置tlog

首先在/internal/config/config.go文件中的Config里添加一下tlog的配置信息

```go
// Package config 提供应用程序配置结构定义
package config

import "gitee.com/dn-jinmin/tlog"

// Config 应用程序配置结构
type Config struct {
    Name string // 应用程序名称
    Addr string // 服务监听地址

    // MongoDB 数据库配置
    Mongo struct {
       User     string   // 用户名
       Password string   // 密码
       Hosts    []string // 主机地址列表
       Port     int      // 端口号
       Database string   // 数据库名
       Params   string   // 连接参数
    }

    // JWT Token 配置
    Jwt struct {
       Secret string // 签名密钥
       Expire int64  // 过期时间（秒）
    }

    Tlog struct {
       Mode  tlog.LogMod // 运行模式
       Label string      // 加载日志输出的标签
    }
}
```

添加完成之后，同样要在api.yml文件中添加对应的配置

```yaml
# AIWorkHelper API 本地开发环境配置文件
# 此配置用于本地开发和测试环境

# 应用程序基本配置
Name: api                    # 应用程序名称
Addr: 0.0.0.0:8888          # API 服务监听地址和端口，0.0.0.0 表示监听所有网络接口

# MongoDB 数据库配置
Mongo:
  User: ""                   # MongoDB 用户名，空字符串表示无认证
  Password: ""               # MongoDB 密码，空字符串表示无认证
  Hosts: ["127.0.0.1"]      # MongoDB 主机列表，支持多个主机用于集群部署
  Port: 27017                # MongoDB 端口号，默认 27017
  Database: "aiworkhelper"   # 使用的数据库名称
  
# JWT Token 配置
Jwt:
  Secret: "kuangbiao.camps"        # JWT 签名密钥，用于 Token 的签名和验证
  Expire: 8640000                 # Token 过期时间（秒），8640000秒 = 100天

# Tlog 分布式链路追踪日志配置，用于统一一次请求的所有日志，通过 trace ID 关联
Tlog:
  Mode: 1                        # 日志模式：0=关闭 1=全部日志 2=Info及以上 3=Warn及以上 4=仅Error
  Label: "aiworkc"               # 日志标签，用于标识日志来源，会在每条日志中添加 tlog_label 字段
```

## 2.2 初始化tlog

完成了tlog的配置之后，然后在`/internal/handler/api/handler.go`文件中对tlog进行初始化。在NewHandle中添加tlog的初始化逻辑。

```go
func NewHandle(svc *svc.ServiceContext) *handle {
    h := &handle{
       srv:  gin.Default(),
       addr: "0.0.0.0:8080",
    }
    if len(svc.Config.Addr) > 0 {
       h.addr = svc.Config.Addr
    }

    tlog.Init(
       tlog.WithLabel(svc.Config.Tlog.Label),
       tlog.WithMode(svc.Config.Tlog.Mode),
    )

    handlers := initHandler(svc)
    for _, handler := range handlers {
       handler.InitRegister(h.srv)
    }

    return h
}
```

## 2.3 添加日志中间件

tlog需要给请求添加一个全链路的traceID，最好的做法就是在中间件里面来实现，跟业务解藕。在请求刚进来的时候就加上traceID，同时还可以记录请求开始时间，响应时间等信息。

在`/internal/middleware/`下添加log.go文件，来实现日志中间件

```go
// Package middleware 提供 HTTP 请求中间件
package middleware

import (
    "fmt"
    "gitee.com/dn-jinmin/tlog"
    "github.com/gin-gonic/gin"
    "time"
)

// Log 日志中间件结构体，用于记录 HTTP 请求的链路追踪日志
type Log struct{}

// NewLog 创建日志中间件实例
func NewLog() *Log {
    return &Log{}
}

// Handler 日志中间件处理函数，为每个 HTTP 请求生成链路追踪日志
func (w *Log) Handler(ctx *gin.Context) {
    startTime := time.Now()                                                   // 记录请求开始时间
    url := fmt.Sprintf("%s:%s", ctx.Request.URL.Path, ctx.Request.Method)    // 构造请求标识：路径:方法

    ctx.Request = ctx.Request.WithContext(tlog.TraceStart(ctx.Request.Context())) // 启动链路追踪，生成 trace ID
    defer func() {
       tlog.InfoCtx(ctx.Request.Context(), url, "time", tlog.RTField(startTime, time.Now())) // 记录请求完成日志和响应时间
    }()

    ctx.Next() // 继续执行后续中间件和处理函数
}
```

完成好日志中间件的开发之后，同样要在`/internal/handler/api/handler.go`文件定义的`handler`中将它个使用起来。在NewHandle函数中添加如下代码

```go
func NewHandle(svc *svc.ServiceContext) *handle {
    h := &handle{
       srv:  gin.Default(),
       addr: "0.0.0.0:8080",
    }
    if len(svc.Config.Addr) > 0 {
       h.addr = svc.Config.Addr
    }

    tlog.Init(
       tlog.WithLabel(svc.Config.Tlog.Label),
       tlog.WithMode(svc.Config.Tlog.Mode),
    )

    h.srv.Use(middleware.NewLog().Handler)

    handlers := initHandler(svc)
    for _, handler := range handlers {
       handler.InitRegister(h.srv)
    }

    return h
}
```

## 2.4 日志与错误异常结合处理

由于gin.Context不能通过`c.next()`获取到执行之后的错误信息。我们可以在httpx包下来实现。在`/pkg/httpx`包下的response.go文件中，有一个`okhandler`方法和`errorhandler`方法。这两个方法是可以自定义的。所以我们可以在`errorhandler`方法中读取到一次请求所产生的错误信息，然后输出到日志中。

可以在`/internal/handler/`下创建一个result.go文件，来实现自定义的errorhandler方法

```go
// Package handler 提供 HTTP 请求处理相关功能
package handler

import (
    "gitee.com/dn-jinmin/tlog"
    "github.com/gin-gonic/gin"
)

// cause 接口定义了获取根本错误原因的方法，用于错误链追踪
type cause interface {
    Cause() error
}

// ErrorHandler 全局错误处理函数，实现 httpx.SetErrorHandler 所需的错误处理接口
// 该函数会被 httpx 包在调用 FailWithErr 时自动调用，用于统一处理应用中的错误
// 参数: ctx - Gin 上下文, err - 需要处理的错误
// 返回: HTTP状态码和处理后的错误对象
func ErrorHandler(ctx *gin.Context, err error) (int, error) {
    var e error
    if ce, ok := err.(cause); ok { // 如果错误实现了 cause 接口，获取根本原因
       e = ce.Cause()
       tlog.ErrorCtx(ctx.Request.Context(), "err", err.Error())
    } else { // 否则直接使用原始错误
       e = err
       tlog.ErrorCtx(ctx.Request.Context(), "err", err.Error())
    }
    return 500, e // 统一返回 500 状态码和错误对象
}
```

同样完成了ErrorHandler之后。需要在`/internal/handler/api/handler.go`文件中的NewHandle中添加进去

```go
func NewHandle(svc *svc.ServiceContext) *handle {
    h := &handle{
       srv:  gin.Default(),
       addr: "0.0.0.0:8080",
    }
    if len(svc.Config.Addr) > 0 {
       h.addr = svc.Config.Addr
    }

    tlog.Init(
       tlog.WithLabel(svc.Config.Tlog.Label),
       tlog.WithMode(svc.Config.Tlog.Mode),
    )

    h.srv.Use(middleware.NewLog().Handler)
    httpx.SetErrorHandler(handler.ErrorHandler)

    handlers := initHandler(svc)
    for _, handler := range handlers {
       handler.InitRegister(h.srv)
    }

    return h
}
```

这里分析一下`errorhandler`的作用机制：

1. httpx 包的错误处理实现：
   - `/internal/handler/result.go`文件中的`ErrorHandler` 函数实现了 `httpx.SetErrorHandler` 所需的接口签名
   - 在 `internal/handler/api/handler.go` 通过 `httpx.SetErrorHandler(handler.ErrorHandler)`这一行代码注册
2. 工作流程：
   - 当应用中调用 `httpx.FailWithErr(ctx, err)` 时
   - httpx 包会自动调用这里注册的 `ErrorHandler` 函数
   - 进行统一的错误处理和状态码设置
3. 错误链处理：
   - 通过 `cause` 接口支持错误链追踪
   - 如果错误实现了 `Cause()` 方法，会获取根本错误原因
   - 这种设计模式常用于 Go 的错误包装和追踪
4. 统一错误响应：
   - 所有错误统一返回 500 状态码
   - 保证了应用错误处理的一致性

这个设计很好地实现了关注点分离：httpx 包负责 HTTP 响应格式化，而具体的错误处理逻辑由应用层的 ErrorHandler 来定制。

# 3. 功能测试

接下来我们进行一次错误请求，来看下错误日志的打印情况，执行以下代码，或者在apipost中去执行

```sql
curl --request POST \
  --url http://localhost:8888/v1/user/login \
  --header 'content-type: application/json' \
  --data '{
    "name": "root1",
    "password": "123456"
}'
```

![BlockNote image](https://ls8sck0zrg.feishu.cn/space/api/box/stream/download/asynccode/?code=NjU0ZDhlZDIxOWU2ZTI3ZGM4ZDQ4MDc4OTNhMmUyMjFfNE5zNFc1WWxRN2lOaGtLdEFMc2phTElTYmtVUll6UlZfVG9rZW46QmZVc2JNeTFUb0xxOW54c1lTMGNWUU5nbm9iXzE3Nzc4Mzc0OTI6MTc3Nzg0MTA5Ml9WNA)

由于系统中没有root1这个用户，我们可以在控制台看到如下信息

```json
{"tlog_id":"ed52ce04bb71b356","tlog_label":"aiworkhelper","tlog_level":"error","tlog_msg":"查询不到该用户","tlog_path":"/Users/xxxx/AI项目/AIWorkHelper/internal/handler/result.go:25","tlog_relatedId":"err"}
{"tlog_id":"ed52ce04bb71b356","tlog_label":"aiworkhelper","tlog_level":"info","tlog_msg":"time","tlog_path":"/Users/xxxx/AI项目/AIWorkHelper/internal/middleware/log.go:26","tlog_relatedId":"/v1/user/login:POST","tlog_rt":"1.682125ms"}
```

![BlockNote image](https://ls8sck0zrg.feishu.cn/space/api/box/stream/download/asynccode/?code=NTZkYTRlNGRmOGY1ZDg0OTM0MTcxN2NmOTA3MmRiNDhfNUxqQWhtVjhnU2lnZ3FtbnZvN01xVmlyTDVkRndwcUdfVG9rZW46R2tkWWJJb3lIb2dzY2J4TmhFaWMwYU9ybnhmXzE3Nzc4Mzc0OTI6MTc3Nzg0MTA5Ml9WNA)

可以看到在两个地方都打印了日志，分别寄了了日志所在的文件行号，有相同的tlog_id。

接下来我们在业务代码中添加一个错误信息，来看下日志的打印效果。在`/internal/model/usermodel.go`文件的`FindByName`方法中，返回的error做如下变更，找不到用户，将请求用户名添加到error信息里

```go
func (m *defaultUserModel) FindByName(ctx context.Context, name string) (*User, error) {
    var user User
    err := m.col.FindOne(ctx, bson.M{"name": name}).Decode(&user)
    switch err {
    case nil:
       return &user, nil
    case mongo.ErrNoDocuments:
       return nil, xerr.WithMessagef(ErrNotUser, "err %v ; name %v", ErrNotUser.Error(), name)
    default:
       return nil, err
    }
}
```

可以看到日志输出：

```json
{"tlog_id":"487ad9593ba98945","tlog_label":"aiworkhelper","tlog_level":"error","tlog_msg":"/Users/gupeng/AI项目/AIWorkHelper/internal/model/usermodel.go:71:err 查询不到该用户 ; name root1","tlog_path":"/Users/xxxx/AI项目/AIWorkHelper/internal/handler/result.go:22","tlog_relatedId":"err"}
{"tlog_id":"487ad9593ba98945","tlog_label":"aiworkhelper","tlog_level":"info","tlog_msg":"time","tlog_path":"/Users/xxxx/AI项目/AIWorkHelper/internal/middleware/log.go:26","tlog_relatedId":"/v1/user/login:POST","tlog_rt":"2.449083ms"}
```

![BlockNote image](https://ls8sck0zrg.feishu.cn/space/api/box/stream/download/asynccode/?code=NDFiNmZiMDVjMjlmODRhMTEwYTZjNzE3NmMzODUzMTJfS0VseURWRHhIc3lpZFBFSmxOdHpaRTBNUzdkWmVnbnpfVG9rZW46RWJkZ2JuZ1FlbzlBdWR4YnJ6RGMwQU1SblFkXzE3Nzc4Mzc0OTI6MTc3Nzg0MTA5Ml9WNA)

可以看到正确抛出了底层的错误信息，并且正确的打印出了底层错误的文件行号位置信息