生成级agent
入口层：鉴权/限流/会话路由
编排层：planner任务拆解，
模型层，工具层：executor模型工具调用
记忆层：memory读写上下文
观测层：安全模块/聚合返回

agent闭环实现：
loop{
planner
executor
reflector
}
安全边界：最大轮次，超市校验，人工兜底

多轮对话里的记忆分级管理，缓存策略设计
- 短期上下文
- 会话摘要
- 用户长期画像
- 业务知识库