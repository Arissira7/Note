 1. 对比思维链（CoT），ReAct范式在模型与外部环境交互过程中，核心解决了哪些痛点问题？
    - CoT 完全依赖内部参数
    - CoT 是开环的，长链条推理中一旦第一步想偏，后面就全错
    - CoT 的输出纯粹是文本，没有真实用处
 2. 详细阐述大模型实现函数调用（Function Call）的完整流程，模型是如何将自然语言语义转化为标准化结构化参数的？
    - 四大完整链路
      - 定义注入：用 JSON Schema 描述工具功能与入参，随用户问题一同输入模型。
      - 模型决策：模型判断需要触发工具，停止输出普通文本，转而输出结构化 JSON 参数。
      - 外部执行：宿主程序解析该 JSON，在本地或调用第三方 API 执行函数，拿到结果。
      - 二次总结：将执行结果拼接回上下文再次传给模型，模型将其“翻译”为人类自然语言输出。
    - 三大转化机制（语义 → 结构化参数）
      - 语义对齐（Attention）：利用 Transformer 的注意力机制，将用户输入的非结构化文本与 JSON Schema 中的参数描述进行高维语义匹配（例如：自动将“北京”映射到 location 字段）。
      - 指令微调（SFT）：模型在训练阶段输入了大量 [对话] + [Schema] -> [JSON 参数] 的微调数据，使其具备强烈的格式先验知识。
      - 约束解码（Constrained Decoding）：在推理时，通过特殊 Token 触发工具模式，并结合语法状态机/Logits 处理器强行限制模型下一个 Token 的生成概率，从底层数学上确保输出 100% 符合 JSON 语法规范。
 3. 实际开发过程中，如何判断一个任务适合采用固定Workflow流程，还是自主决策型Agent来实现？
 4. 详细讲解Agent系统中长短期记忆的设计逻辑，在长时对话场景下，如何实现上下文信息的动态压缩与选择性遗忘？
    记忆架构逻辑
    - 短期记忆（内存在线）： 依托LLM上下文窗口，负责即时推理。受Token硬限制，随对话推进不断向后滑动。
    - 长期记忆（硬盘离线）： 依托向量或图数据库，负责沉淀海量历史。通过“检索-注入”机制按需唤醒。

    上下文动态压缩（解决Token爆炸）
    - 滚动循环摘要： 当对话达到Token阈值，后台异步将最老的一段明细压缩为“历史概述”，并与新明细拼接（历史摘要 + 近期明细）。
    - 结构化标签提取： 丢弃无用的寒暄文本，直接提取高信息密度的键值对（KV）或用户画像（例如：{饮食偏好: 控糖}），实现极致压缩。

    选择性遗忘机制（解决记忆污染）
    - 系统通过评估记忆保留得分决定信息的留存与消亡：
      - 记忆得分=初始重要度×访问频率×时间衰减因子
    - 软遗忘（降级）： 得分较低的记忆，移出高频检索区，转入冷存储并降低召回权重。
    - 硬遗忘（删除）： 评分触底、存储空间不足或用户要求时，执行物理删除。
    - 主动覆盖（擦除）： 检测到新旧事实冲突（如：旧记忆“单身” vs 新输入“结婚”），直接擦除或版本覆盖旧记忆。
 5. 在Agent系统架构中，RAG检索获取的外部知识，是如何精准输入给模型，同时避免无关知识对模型输出产生干扰的？
 6. 解释重排序（Rerank）技术的定义，它在Agent检索链路中，对提升模型最终决策准确性起到哪些关键作用？
 7. 单Agent处理复杂长周期任务时，引入多智能体（Multi Agent）架构的核心优势有哪些？
 8. Multi Agent系统中的路由（Router）节点，依据哪些规则和逻辑，将任务分发给对应的子Agent？
 9. 什么是人在回路（Human-in-the-loop）机制？在Agent系统开发中，如何合理设计人工审批断点，保障系统运行可靠性？
10. 
11. 针对Agent多步推理过程中产生的响应延迟问题，可落地的优化方法有哪些？
12. 随着大模型上下文窗口持续扩容，传统RAG技术是否会被完全替代？阐述你的核心观点及理由
13. 简要说一下Milvus和ES这两个向量数据库的底层
    - 简单来说：Milvus 是“为向量而生”的专用型向量数据库，而 ES 是“从传统文本检索延伸而来”的通用搜索引擎。
    - 底层：
      - Milvus 的底层架构：专为向量设计的存算分离
        - Milvus 从第一天起就是为了海量向量数据而设计的，采用了云原生（Cloud-Native）和存算分离的微服务架构。
        - 解耦的微服务：Milvus 将系统拆分为四大层次：接入层（Proxy）、协调服务（Coordinator）、执行节点（Worker Node）和存储层。
        - 计算与索引分离：负责写入数据的 DataNode、负责构建索引的 IndexNode 和负责查询的 QueryNode 都是完全独立的。这意味着你可以单独为“建索引”或“查询”扩容。
        - 向量执行引擎（Knowhere）：Milvus 的核心计算层是一个名为 Knowhere 的底层库，它对上层屏蔽了具体的向量算法细节，对下层封装了诸如 Faiss、HNSW、ScaNN、DiskANN 等顶尖的向量索引库，并针对 SIMD 指令集（如 AVX512）和 GPU 加速 进行了深度优化。
        - 数据存储与分片：数据在底层被切分为多个 Segment（段）。
        - Milvus 将元数据存在 etcd，日志存在对象存储（如 MinIO/S3）或消息队列（如 Pulsar/Kafka）中。
        - 查询时，QueryNode 直接从对象存储加载编译好的向量索引到内存中进行流式计算。
      - Elasticsearch 的底层架构：基于 Lucene 的倒排与全文本延伸
        - ES 的向量检索能力是建立在 Apache Lucene 引擎之上的。它试图在原有的全文检索生态里，完美兼容向量这个“新成员”。
        - 基于 Lucene Segments：ES 的数据存储以 Shard（分片）为单位，每个分片底层是一个 Lucene 实例，由多个只读的 Segment（段）组成。当引入向量（dense_vector 类型）时，向量数据和索引也是跟着 Lucene Segment 一起生成和刷盘（Flush）的。
        - 原生 HNSW 算法实现：从 Lucene 9.x 开始，Lucene 底层原生实现了 HNSW（Hierarchical Navigable Small World） 图算法。ES 借此将向量的图结构直接存储在 Lucene 的高级文件格式（如 .vec 和 .vex 文件）中。
        - 缓存与内存依赖：因为 Lucene 依赖操作系统的文件系统缓存（Page Cache）来加速读取，为了保证 HNSW 的图检索速度，ES 通常需要将整个 HNSW 图完整加载到物理内存中。如果内存不足触发频繁的磁盘 I/O，向量检索性能会断崖式下跌。


