用户模块是这个项目基石。后续的一切业务逻辑，包括常规的办公业务以及AI职能业务，都是以用户为维度进行的。

# 1. 业务分析

用户模块的核心业务主要包含以下几个方面：

**注册：**新用户创建，即在数据库里创建一个新的用户记录，并允许用户完善个人信息。

**登陆**：已完成注册的用户登陆系统

**鉴权**：用户登录成功后，系统需要发一个“通行证”（也就是Token），后续用户访问需要权限的接口时，都得带着这个“通行证”，系统要能识别并验证它的有效性。

# 2. 构建服务

这里我们创建一个空目录AIWorkHelper作为我们的项目根目录。使用 `goctl-gin`来进行项目构建

## 2.1 **编写 API 文件**。

在 `.api` 文件中定义服务的路由、请求参数、响应数据结构以及用户模型数据信息。在项目根目录创建doc目录，并且在doc目录下新建三个api文件，base.api，user.api，api.api文件，其中api.api是整个程序的入口，会引入base.api和user.api。

结构如下：

```sh
AIWorkHelper/ 
└── doc/
    ├── api.api
    ├── base.api
    └── user.api
```

1. base.api

```go
// 基础通用数据结构
type (
      // 路径参数请求 - 用于 GET /user/：id 等
      IdPathReq {
          Id string `uri："id，omitempty"`
      }

      // ID 响应 - 返回操作后的资源ID
      IdResp {
          Id string `json："id，omitempty"`
      }
)
```

- user.api

```go
// 用户模块接口定义
syntax = "v1"

info (
    title： "主体业务后台"
    author： "鹏哥"
)

type (
    // 用户基础信息
    User {
       Id            string `json："id，omitempty"`        // 用户ID
       Password      string `json："password，omitempty"`  // 密码
       Name          string `json："name，omitempty"`      // 用户名
       Status        int    `json："status，omitempty"`    // 状态：0=禁用 1=启用
    }

    // 登录请求
    loginReq {
       Name string `json："name，omitempty"`        // 用户名
       Password string `json："password，omitempty"` // 密码
    }

    // 登录响应
    loginResp {
       Status       int    `json："status，omitempty"`       // 状态：0=失败 1=成功
       Id           string `json："id，omitempty"`           // 用户ID
       Name         string `json："name，omitempty"`         // 用户名
       AccessToken  string `json："token，omitempty"`        // JWT令牌
       AccessExpire int64  `json："accessExpire，omitempty"` // 过期时间
       RefreshAfter int64  `json："refreshAfter，omitempty"` // 刷新时间
    }

    // 用户列表查询请求
    userListReq {
        Ids   []string `json："ids，omitempty"`   // 用户ID列表
        Name  string   `json："name，omitempty"`  // 用户名模糊搜索
        Page  int      `json："page，omitempty"`  // 页码
        Count int      `json："count，omitempty"` // 每页数量
    }

    // 用户列表响应
    userListResp {
        Count int64   `json："count"` // 总记录数
        List  []*User `json："data"`  // 用户列表
    }

    // 修改密码请求
    updatePasswordReq {
        Id     string `json："id"`     // 用户ID
        OldPwd string `json："oldPwd"` // 原密码
        NewPwd string `json："newPwd"` // 新密码
    }
)

// 用户服务 - 公开接口
@server(
    group： v1/user
    logic： User
)
service User {
    @server(
        handler： Login
        logic： User.Login
        doc： 用户登录
        logicDoc： 验证用户名密码
    )
    post /login(loginReq) returns (loginResp)
}

// 用户服务 - 需要认证
@server(
    group： v1/user
    logic： User
    middleware： Jwt
)
service User{
    @server(
        handler： Info
        logic： User.Info
        doc： 获取用户信息
        logicDoc： 根据ID获取用户
    )
    get /：id(IdPathReq) returns(User)

    @server(
        handler： Create
        logic： User.Create
        doc： 创建用户
        logicDoc： 新增用户
    )
    post / (User)

    @server(
        handler： Edit
        logic： User.Edit
        doc： 编辑用户
        logicDoc： 更新用户信息
    )
    put / (User)

    @server(
        handler： Delete
        logic： User.Delete
        doc： 删除用户
        logicDoc： 删除指定用户
    )
    delete /：id(IdPathReq)

    @server(
        handler： List
        logic： User.List
        doc： 用户列表
        logicDoc： 分页查询用户
    )
    get /list(userListReq) returns (userListResp)

    @server(
        handler： UpdatePassword
        logic： User.UpdatePassword
        doc： 修改密码
        logicDoc： 更新用户密码
    )
    post /password(updatePasswordReq)
}
```

api.api

```java
// API 主入口文件
syntax = "v1"

// 导入模块
import "base.api"  // 基础数据结构
import "user.api"  // 用户相关接口

// 项目配置
info (
    title： "后台系统admin"
    author： "鹏哥"
    serviceMod： 1     // 1=增量更新 0=覆盖模式
)
```

## 2.2 **通过**`goctl-gin`**工具生成项目代码**

执行以下命令生成项目代码

```go
goctl-gin api go -api ./doc/api.api -dir ./
```

生成的项目目录结构大致如下：

```text
AIWorkHelper/
├── doc/
├── api/
│   ├── user.api
│   └── base.api
├── etc/
├── internal/
└── pkg/
```

# 3. 业务实现

## 3.1 定义数据模型

由于整个项目的底层存储用到的是Mongodb，上一步在user.api里我们已经定义了user的数据结构，这里我们要执行以下命令生成底层Mongodb中存储的user数据模型，可以理解Gorm框架里对应的一张Mysql的user表结构

```go
goctl-gin model mongo --type user --dir ./internal/model/
```

执行完上述命令之后，可以在项目根目录的/internal/model/目录下生成以下几个文件

```go
model
├── error.go
├── tool.go
├── usermodel.go
└── usertypes.go
```

其中usertypes.go中关于user的代码如下，

```text
type User struct {
    ID primitive.ObjectID `bson："_id，omitempty" json："id，omitempty"`
    // TODO： Fill your own fields

    UpdateAt int64 `bson："updateAt，omitempty" json："updateAt，omitempty"`
    CreateAt int64 `bson："createAt，omitempty" json："createAt，omitempty"`
}
```

我们需要根据user.api文件中的user属性补充完整

```text
type User struct {
    ID primitive.ObjectID `bson："_id，omitempty" json："id，omitempty"`
    // TODO： Fill your own fields

    Name     string `bson："name" json："name"`
    Password string `bson："password" json："password"`
    Status   int    `bson："status" json："status"`
    IsAdmin  bool   `bson："isAdmin" json："isAdmin"` // 是否为管理员
    UpdateAt int64  `bson："updateAt" json："updateAt"`
    CreateAt int64  `bson："createAt" json："createAt"`
}
```

接下来就可以运行一下`go mod`命令来加载项目相关依赖了

```go
go mod tidy
```

执行完了之后，可以看到我们的项目结构如下如所示，添加完依赖之后，代码上的标红错误都已经消失

![BlockNote image](https://ls8sck0zrg.feishu.cn/space/api/box/stream/download/asynccode/?code=MDkyZmE2YmUwNzU3OGRiMzZhZmM1NmQxMzZhMzgwNGRfQWNaMW54U2VqcWV2ZktjellBQWN6Slc5Ukc1SGhIaUVfVG9rZW46SVR5dmJXWTkzb0d6NjN4MkVpYWNwYlo0bnZiXzE3Nzc4MzczNzM6MTc3Nzg0MDk3M19WNA)

## 3.2 添加用户方法

在完善了user的用户模型之后，我们需要在usermode.go文件中添加对应的方法，前面通过goctl-gin这个工具生成user数据模型的时候，在usermode.go文件中只是默认生成了以下四个方法：

```go
type UserModel interface {
    Insert(ctx context.Context， data *User) error
    FindOne(ctx context.Context， id string) (*User， error)
    Update(ctx context.Context， data *User) error
    Delete(ctx context.Context， id string) error
}
```

这里的用户登陆和注册都需要根据用户名去查询的需求，所以这里需要添加根据用户名去查询的方法`findByName`

```go
func (m *defaultUserModel) FindByName(ctx context.Context， name string) (*User， error) {
    var user User
    err ：= m.col.FindOne(ctx， bson.M{"name"： name}).Decode(&user)
    switch err {
    case nil：
       return &user， nil
    case mongo.ErrNoDocuments：
       return nil， ErrNotFound
    default：
       return nil， err
    }
}  
```

## 3.3 完善错误类型

在上述添加完FindByName方法之后，发现返回错误都是ErrNotFound，由于后续还有其他的数据模型需要查询返回，而不只是返回user模型，所以这里我们需要扩展错误类型。直接在model目录下的error.go文件最上添加用户查询不到的错误信息

```go
var (
    ErrNotFound        = mongo.ErrNoDocuments
    ErrInvalidObjectId = errors.New("invalid objectId")
    ErrNotUser         = errors.New("查询不到该用户")
)
```

![BlockNote image](https://ls8sck0zrg.feishu.cn/space/api/box/stream/download/asynccode/?code=OWQyOGJiMTMzN2RiNzcxMGM4ZjAyMTk4MmZmZDU4MWNfTnVSY0pxanVNbDRKUzdTYWFYQ0M2enJ1VlJMMXF4ZGxfVG9rZW46T0ZjNGJuMVdVb1pRZEF4MTZhNGN4S2FNbkNiXzE3Nzc4MzczNzM6MTc3Nzg0MDk3M19WNA)

定义完成之后， 就可以将usermode.go文件中，原本查询用户查询不到的错误由`ErrNotFound`修改为`ErrNotUser`

![BlockNote image](https://ls8sck0zrg.feishu.cn/space/api/box/stream/download/asynccode/?code=MzllOTRkMDIzYjdmZTI4MjczYjY1MTUzNjFjYWMwOWVfRDltMjVqcEtBb05BTFFnWTZaSmlpaDlXaEVGZ2tLeHpfVG9rZW46R0ZBOGJ2RVBEbzRQVEF4cjdMUmNYeTFrbnRlXzE3Nzc4MzczNzM6MTc3Nzg0MDk3M19WNA)

## 3.4 MongoDB的yml配置

1. 首先，在配置文件 `etc/local/api.yaml` 中添加MongoDB的配置信息，注意前面我们安装Mongodb的时候使用的命令是

```go
docker run -d --name aiworkhelper-mongodb -p 27017：27017 mongo：7.0
```

没有使用用户名和密码，所以这里用户名和密码直接配置为空字符串即可。我们使用本地的docker启动的Mongodb，所以Hosts配置为127.0.0.1，端口27017，配置完成之后的api.yml文件如下：

```yaml
# 应用程序基本配置
Name： AIWorHelper           # 应用程序名称
Addr： 0.0.0.0：8888          # API 服务监听地址和端口，0.0.0.0 表示监听所有网络接口

# MongoDB 数据库配置
Mongo：
  User： ""                   # MongoDB 用户名，空字符串表示无认证
  Password： ""               # MongoDB 密码，空字符串表示无认证
  Hosts： ["127.0.0.1"]       # MongoDB 主机列表，支持多个主机用于集群部署
  Port： 27017                # MongoDB 端口号，默认 27017
  Database： "aiworkhelper"   # 使用的数据库名称
```

**注意：** 在本地开发环境中，如果你的MongoDB没有设置用户名和密码，可以将 `Username` 和 `Password` 留空。

但在正式的生产环境中，**必须**设置复杂的用户名和密码以保证数据库安全。

## 3.5 MongoDB的Config配置

在`internal/config/config.go`文件中包含着项目的全局配置，对应我们api.yml文件中的配置，这里我们把刚刚配置的MongoDB添加进去即可，修改完成之后如下：

```go
package config

type Config struct {
    Name string
    Addr string

    Mongo struct {
       User     string   //用户名
       Password string   //密码
       Hosts    []string //host地址
       Port     int      //端口
       Database string   //数据库名
       Params   string   //其他相关认证
       //MaxPoolSize uint64   //连接池最大数
    }
}
```

## 3.6 引入MongoDB工具包

我们在访问`mongodb`的时候，通常是以url的形式来访问，如 `mongodb：//localhost：27017/aiwork_db`。所以给予上面的`mongodb`的配置信息，我们还需要将其拼接成一个`mongodb`的访问url，以及我们访问成功之后还需要获取到连接到`mongodb`的客户端等信息，这些功能我们都可以封装到一个`mongodb`的工具包中，这里我们在`internal/pkg`目录下创建一个`mongoutil`目录，在`mongoutil`目录下分别创建`mongoutil.go`来实现`mongodb`的一些工具方法

mongoutil.go

```go
// Package mongoutil 提供 MongoDB 连接和配置工具
package mongoutil

import (
    "context"
    "fmt"
    "go.mongodb.org/mongo-driver/mongo"
    "go.mongodb.org/mongo-driver/mongo/options"
)

// MongodbConfig MongoDB 连接配置
type MongodbConfig struct {
    User        string   // 用户名
    Password    string   // 密码
    Hosts       []string // 主机地址列表
    Port        int      // 端口号
    Database    string   // 数据库名
    Params      string   // 连接参数
    MaxPoolSize uint64   // 连接池最大连接数
}

// MongodbDatabase 创建 MongoDB 数据库连接
func MongodbDatabase(config *MongodbConfig) (*mongo.Database， error) {
    // 连接 MongoDB
    client， err ：= mongo.Connect(context.TODO()， config.GetApplyURI()...)
    if err != nil {
       return nil， err
    }

    // 测试连接
    err = client.Ping(context.TODO()， nil)
    if err != nil {
       return nil， err
    }

    return client.Database(config.Database)， nil
}

// GetApplyURI 构建 MongoDB 连接选项
func (t *MongodbConfig) GetApplyURI() []*options.ClientOptions {
    var ops []*options.ClientOptions
    var uri string

    // 构建基础 URI
    uri = "mongodb：//"

    // 添加认证信息
    if len(t.User) > 0 && len(t.Password) > 0 {
       uri = fmt.Sprintf("%v%v：%v@"， uri， t.User， t.Password)
    }

    // 添加主机列表
    for index， v ：= range t.Hosts {
       var host string
       if t.Port != 0 {
          host += v + fmt.Sprintf("：%d"， t.Port)
       } else {
          host = v
       }
       if index < len(t.Hosts)-1 {
          host += "，"
       }
       uri += host
    }

    // 添加数据库名
    uri += fmt.Sprintf("/%s"， t.Database)

    // 添加连接参数
    if len(t.Params) > 0 {
       uri = fmt.Sprintf("%v?%v"， uri， t.Params)
    }

    // 设置 URI 选项
    ops = append(ops， options.Client().ApplyURI(uri))

    // 设置连接池大小
    if t.MaxPoolSize > 0 {
       ops = append(ops， options.Client().SetMaxPoolSize(t.MaxPoolSize))
    }

    return ops
}
```

## 3.7 完善ServiceContext

配置好之后，项目启动时需要根据配置信息创建数据库连接。这个通常在`internal/svc/servicecontext.go` 文件中完成。`ServiceContext` 是一个很重要的结构体，它像一个“服务上下文容器”，存放着整个服务运行期间需要共享的资源，比如数据库连接、配置信息、各种模型实例等。这里我们需要修改`ServiceContext`结构，添加mongodb以及user相关实例。

修改完成之后的`servicecontext.go`

```go
// Package svc 提供服务上下文和依赖注入
package svc

import (
    "AIWorkHelper/internal/config"
    "AIWorkHelper/internal/model"
    "AIWorkHelper/pkg/mongoutil"
    "go.mongodb.org/mongo-driver/mongo"
)

// ServiceContext 服务上下文，管理所有依赖组件
type ServiceContext struct {
    Config config.Config // 应用配置

    // 数据库和模型
    Mongo     *mongo.Database   // MongoDB 数据库实例
    UserModel model.UserModel   // 用户模型
}

// NewServiceContext 创建服务上下文实例
func NewServiceContext(c config.Config) (*ServiceContext， error) {
    // 初始化 MongoDB 连接
    mongoDb， err ：= mongoutil.MongodbDatabase(&mongoutil.MongodbConfig{
       User：     c.Mongo.User，
       Password： c.Mongo.Password，
       Hosts：    c.Mongo.Hosts，
       Port：     c.Mongo.Port，
       Database： c.Mongo.Database，
    })
    if err != nil {
       return nil， err
    }

    // 构建服务上下文
    svc ：= &ServiceContext{
       Config：    c，
       Mongo：     mongoDb，
       UserModel： model.NewUserModel(mongoDb)， // 初始化用户模型
    }

    return svc， nil
}
```

## 3.8 用户模型初始化

一个新系统上线时，用户表里是空的，连个管理员都没有。所以，我们需要一个初始化逻辑：在服务第一次启动时，检查数据库里有没有系统管理员，如果没有，就自动创建一个默认的管理员账号。

这个逻辑同样可以放在 `ServiceContext` 的初始化过程中。可以在 `servicecontext.go` 中再增加一个函数。

```go
// initUser 初始化系统管理员用户
func initUser(svc *ServiceContext) error {
    ctx ：= context.Background()

    // 检查是否已存在管理员用户
    systemUser， err ：= svc.UserModel.FindAdminUser(ctx)
    if err != nil && err != model.ErrNotUser {
       return err
    }

    // 如果管理员用户已存在，直接返回
    if systemUser != nil {
       return nil
    }

    // 创建默认管理员用户
    return svc.UserModel.Insert(ctx， &model.User{
       Name：     "root"，                                                           // 默认管理员用户名
       Password： "$2a$10$ddIvqt7U6zNA9poys.FNCuEZTJY6V.axWy4P7A44TuT9KBegGZlD6"， // 加密后的密码
       Status：   0，                                                               // 用户状态
       IsAdmin：  true，                                                            // 管理员标识
    })
}
```

然后在 `NewServiceContext` 中调用它，注意上一步我们`servicecontext.go` 文件的NewServiceContext函数最后是`return svc， nil`，返回一个`ServiceContext`结构和`nil`，这里我们可以把nil改为用户的初始化函数皆可做到在系统启动时初始化用户数据

```go
// NewServiceContext 创建服务上下文实例
func NewServiceContext(c config.Config) (*ServiceContext， error) {
    // 初始化 MongoDB 连接
    mongoDb， err ：= mongoutil.MongodbDatabase(&mongoutil.MongodbConfig{
       User：     c.Mongo.User，
       Password： c.Mongo.Password，
       Hosts：    c.Mongo.Hosts，
       Port：     c.Mongo.Port，
       Database： c.Mongo.Database，
    })
    if err != nil {
       return nil， err
    }

    // 构建服务上下文
    svc ：= &ServiceContext{
       Config：    c，
       Mongo：     mongoDb，
       UserModel： model.NewUserModel(mongoDb)， // 初始化用户模型
    }

    return svc， initUser(svc)  // 系统启动时初始化用户数据
}
```

注意这里我们在initUser函数里用到了UserModel的FindAdminUser方法，查找系统管理员方法。所以在完成initUser函数之前，我们需要到`internal/model/usermodel.go`文件中添加对应方法

```go
func (m *defaultUserModel) FindAdminUser(ctx context.Context) (*User， error) {
    var data User
    err ：= m.col.FindOne(ctx， bson.M{"isAdmin"： true}).Decode(&data)
    switch err {
    case nil：
       return &data， nil
    case mongo.ErrNoDocuments：
       return nil， ErrNotUser
    default：
       return nil， err
    }
}
```

并将其加入到UserModel接口内

```go
type UserModel interface {
    Insert(ctx context.Context， data *User) error
    FindOne(ctx context.Context， id string) (*User， error)
    Update(ctx context.Context， data *User) error
    Delete(ctx context.Context， id string) error
    FindAdminUser(ctx context.Context) (*User， error)
}
```

## 3.9 JWT鉴权实现

用户的登录和鉴权我们采用JWT来处理，首先用户登陆设计到用户密码，用户密码的存储都是要加密的，所以这里需要用到加解密的方法，我们首先来完成。在`/pkg/`

目录下新建一个目录encrypt，这个包主要用于加解密。在encrypt下新建encrypt.go文件。完成对应方法

```go
pkg
├── conf
│   └── config.go
├── encrypt
│   └── encrypt.go
├── httpx
│   ├── request.go
│   └── response.go
├── mongoutil
│   └── mongoutil.go
└── xerr
    └── xerr.go
```

encrypt.go

```go
package encrypt

import "golang.org/x/crypto/bcrypt"

// hash加密
func GenPasswordHash(password []byte) ([]byte， error) {
    return bcrypt.GenerateFromPassword(password， bcrypt.DefaultCost)
}

// hash校验
func ValidatePasswordHash(password string， hashed string) bool {
    if err ：= bcrypt.CompareHashAndPassword([]byte(hashed)， []byte(password)); err != nil {
       return false
    }
    return true
}
```

另外还需要添加一个token处理的相关包，比如用作jwt token的生成，解析验证，获取，填入context上下文等等。这里我们同样在`/pkg/`目录下再单独创建一个token目录，这个包主要是用来负责进行token相关的一些操作。在token目录下创建ctxtoken.go和token.go文件。关于这两个文件内各个函数的作用这里就不再赘述，代码注释很详细

token.go

```go
// Package token 提供 JWT Token 解析和验证功能
package token

import (
    "context"
    "errors"
    "net/http"
    "strings"

    "github.com/golang-jwt/jwt"
)

// JWT 标准声明字段常量
const (
    jwtAudience   = "aud"           // 受众
    jwtExpire     = "exp"           // 过期时间
    jwtId         = "jti"           // JWT ID
    jwtIssueAt    = "iat"           // 签发时间
    jwtIssuer     = "iss"           // 签发者
    jwtNotBefore  = "nbf"           // 生效时间
    jwtSubject    = "sub"           // 主题
    Authorization = "Authorization" // HTTP 头字段名
)

// Token 解析相关错误定义
var (
    ErrTokenNotFound = errors.New("token不存在")
    ErrTokenInvalid  = errors.New("token is invalid")
    ErrClaimsInvalid = errors.New("invalid token claims")
)

// Parse JWT Token 解析器
type Parse struct {
    AccessSecret string // JWT 签名密钥
}

// NewTokenParse 创建 Token 解析器实例
func NewTokenParse(secret string) *Parse {
    return &Parse{AccessSecret： secret}
}

// Parse 从 HTTP 请求中解析 JWT Token
func (p *Parse) Parse(r *http.Request) (jwt.MapClaims， string， error) {
    // 从请求头提取 Token
    tokenStr ：= p.extractTokenFromHeader(r)
    if len(tokenStr) == 0 {
       return nil， tokenStr， ErrTokenNotFound
    }
    return p.ParseToken(tokenStr)
}

// ParseToken 解析 JWT Token 字符串
func (p *Parse) ParseToken(tokenStr string) (jwt.MapClaims， string， error) {
    // 解析 Token
    token， err ：= jwt.Parse(tokenStr， func(token *jwt.Token) (interface{}， error) {
       return []byte(p.AccessSecret)， nil
    })
    if err != nil {
       return nil， tokenStr， err
    }

    // 验证 Token 有效性
    if !token.Valid {
       return nil， tokenStr， ErrTokenInvalid
    }

    // 提取声明信息
    claims， ok ：= token.Claims.(jwt.MapClaims)
    if !ok {
       return nil， tokenStr， ErrClaimsInvalid
    }

    // 验证声明有效性
    if err = claims.Valid(); err != nil {
       return nil， tokenStr， err
    }
    return claims， tokenStr， nil
}

// ParseWithContext 解析 Token 并将用户信息注入到请求上下文
func (p *Parse) ParseWithContext(r *http.Request) (*http.Request， error) {
    // 解析 Token
    claims， tokenStr， err ：= p.Parse(r)
    if err != nil {
       return r， err
    }

    // 将自定义声明注入上下文
    ctx ：= r.Context()
    for k， v ：= range claims {
       switch k {
       case jwtAudience， jwtExpire， jwtId， jwtIssueAt， jwtIssuer， jwtNotBefore， jwtSubject：
          // 忽略 JWT 标准声明
       default：
          ctx = context.WithValue(ctx， k， v)
       }
    }

    // 将原始 Token 字符串也保存到上下文
    ctx = context.WithValue(ctx， Authorization， tokenStr)

    return r.WithContext(ctx)， nil
}

// extractTokenFromHeader 从 HTTP 请求头中提取 Token
func (p *Parse) extractTokenFromHeader(r *http.Request) string {
    authHeader ：= r.Header.Get("Authorization")
    if authHeader == "" {
       return ""
    }

    // Token 格式应为 "Bearer <token>"
    parts ：= strings.Split(authHeader， " ")
    if len(parts) != 2 || parts[0] != "Bearer" {
       return authHeader
    }

    return parts[1]
}

// GetTokenStr 从上下文中获取原始 Token 字符串
func GetTokenStr(ctx context.Context) string {
    tokenStr， ok ：= ctx.Value(Authorization).(string)
    if !ok {
       return ""
    }
    return tokenStr
}
```

ctxtoken.go

```go
// Package token 提供 JWT Token 生成和用户信息提取功能
package token

import (
    "context"
    "github.com/golang-jwt/jwt"
)

// Identify JWT Token 中用户标识的键名
const Identify = "imooc.com"

// GetJwtToken 生成 JWT Token
// secretKey： 签名密钥
// iat： 签发时间戳
// seconds： 有效期（秒）
// uid： 用户ID
func GetJwtToken(secretKey string， iat， seconds int64， uid string) (string， error) {
    claims ：= make(jwt.MapClaims)
    claims["exp"] = iat + seconds // 过期时间
    claims["iat"] = iat           // 签发时间
    claims[Identify] = uid        // 用户标识

    // 创建 Token 并签名
    token ：= jwt.New(jwt.SigningMethodHS256)
    token.Claims = claims
    return token.SignedString([]byte(secretKey))
}

// GetUId 从上下文中获取用户ID
func GetUId(ctx context.Context) string {
    var uid string
    if jsonUid， ok ：= ctx.Value(Identify).(string); ok {
       uid = jsonUid
    }
    return uid
}
```

在前面两个工具包完成之后，就可以在代码中完成jwt的实例化配置了。前面我们通过`goctl-gin` 生成的代码中已经包含了JWT中间件的框架。可以在`internal/middleware/jwt.go`中查看生成的jwt代码。

```go
package middleware

import "github.com/gin-gonic/gin"

type Jwt struct{}

func NewJwt() *Jwt {
    return &Jwt{}
}

func (m *Jwt) Handler(ctx *gin.Context) {
    ctx.Next()
}
```

但是这里只是个生成模板，我们还需要将相关的key信息添加进来。所以完善`jwt.go`文件，添加所需要的密钥等信息

```go
// Package middleware 提供 HTTP 中间件功能
package middleware

import (
    "AIWorkHelper/pkg/httpx"
    "AIWorkHelper/pkg/token"
    "github.com/gin-gonic/gin"
)

// Jwt JWT 认证中间件
type Jwt struct {
    tokenParser *token.Parse // Token 解析器
}

// NewJwt 创建 JWT 中间件实例
func NewJwt(secret string) *Jwt {
    return &Jwt{
       tokenParser： token.NewTokenParse(secret)，
    }
}

// Handler JWT 认证处理器
func (m *Jwt) Handler(ctx *gin.Context) {
    // 解析 Token 并注入用户信息到请求上下文
    r， err ：= m.tokenParser.ParseWithContext(ctx.Request)
    if err != nil {
       // Token 解析失败，返回错误并终止请求
       httpx.FailWithErr(ctx， err)
       ctx.Abort()
       return
    }

    // 更新请求上下文，继续处理
    ctx.Request = r
    ctx.Next()
}
```

同样这里在`NewJwt`函数中传入的密钥secret信息是需要我们在配置文件api.yml中去配置的，所以这里同样需要我们在`internal/config/config.go`文件中，在Config结构中添加Jwt相关的配置实例

```go
package config

type Config struct {
    Name string
    Addr string

    Mongo struct {
       User     string   //用户名
       Password string   //密码
       Hosts    []string //host地址
       Port     int      //端口
       Database string   //数据库名
       Params   string   //其他相关认证
       //MaxPoolSize uint64   //连接池最大数
    }

    Jwt struct {
       Secret string //签名密钥
       Expire int64  //过期时间
    }
}
```

接着在api.yml文件中加上jwt配置

```yaml
# AIWorkHelper API 本地开发环境配置文件
# 此配置用于本地开发和测试环境

# 应用程序基本配置
Name： api                    # 应用程序名称
Addr： 0.0.0.0：8888          # API 服务监听地址和端口，0.0.0.0 表示监听所有网络接口

# MongoDB 数据库配置
Mongo：
  User： ""                   # MongoDB 用户名，空字符串表示无认证
  Password： ""               # MongoDB 密码，空字符串表示无认证
  Hosts： ["127.0.0.1"]      # MongoDB 主机列表，支持多个主机用于集群部署
  Port： 27017                # MongoDB 端口号，默认 27017
  Database： "aiworkhelper"   # 使用的数据库名称
  
# JWT Token 配置
Jwt：
  Secret： "kuangbiao.camps"        # JWT 签名密钥，用于 Token 的签名和验证
  Expire： 8640000                 # Token 过期时间（秒），8640000秒 = 100天
```

同样我们需要先将jwt实例添加到`ServiceContext`结构中，在`internal/svc/servicecontext.go`文件中来完成

```go
type ServiceContext struct {
    Config config.Config // 应用配置

    // 数据库和模型
    Mongo     *mongo.Database // MongoDB 数据库实例
    UserModel model.UserModel // 用户模型

    // 中间件
    Jwt *middleware.Jwt // JWT 认证中间件
}
```

这里添加完之后，同样在NewServiceContext函数返回的时候也要做jwt的初始化

```go
// NewServiceContext 创建服务上下文实例
func NewServiceContext(c config.Config) (*ServiceContext， error) {
    // 初始化 MongoDB 连接
    mongoDb， err ：= mongoutil.MongodbDatabase(&mongoutil.MongodbConfig{
       User：     c.Mongo.User，
       Password： c.Mongo.Password，
       Hosts：    c.Mongo.Hosts，
       Port：     c.Mongo.Port，
       Database： c.Mongo.Database，
    })
    if err != nil {
       return nil， err
    }

    // 构建服务上下文
    svc ：= &ServiceContext{
       Config：    c，
       Mongo：     mongoDb，
       UserModel： model.NewUserModel(mongoDb)，    // 初始化用户模型
       Jwt：       middleware.NewJwt(c.Jwt.Secret)， // 初始化 JWT 中间件
    }

    // 初始化系统用户数据
    if err ：= initUser(svc); err != nil {
       return nil， err
    }

    return svc， nil
}
```

## 3.10 业务实现

完成了底层的基础配置功能之后，我们就可以在`internal/logic/`下完成用户相关的业务逻辑了，用户相关的业务逻辑在`internal/logic/user.go`文件中。

```go
package logic

import (
    "AIWorkHelper/pkg/encrypt"
    "AIWorkHelper/pkg/token"
    "context"
    "errors"
    "time"

    "AIWorkHelper/internal/domain"
    "AIWorkHelper/internal/svc"
)

type User interface {
    // 验证用户名密码
    Login(ctx context.Context， req *domain.LoginReq) (resp *domain.LoginResp， err error)
    // 根据ID获取用户
    Info(ctx context.Context， req *domain.IdPathReq) (resp *domain.User， err error)
    // 新增用户
    Create(ctx context.Context， req *domain.User) (err error)
    // 更新用户信息
    Edit(ctx context.Context， req *domain.User) (err error)
    // 删除指定用户
    Delete(ctx context.Context， req *domain.IdPathReq) (err error)
    // 分页查询用户
    List(ctx context.Context， req *domain.UserListReq) (resp *domain.UserListResp， err error)
    // 更新用户密码
    UpdatePassword(ctx context.Context， req *domain.UpdatePasswordReq) (err error)
}

type user struct {
    svcCtx *svc.ServiceContext
}

func NewUser(svcCtx *svc.ServiceContext) User {
    return &user{
       svcCtx： svcCtx，
    }
}

// 用户登陆
func (l *user) Login(ctx context.Context， req *domain.LoginReq) (resp *domain.LoginResp， err error) {
    return
}
// 根据ID获取用户
func (l *user) Info(ctx context.Context， req *domain.IdPathReq) (resp *domain.User， err error) {
    return
}

// 新增用户
func (l *user) Create(ctx context.Context， req *domain.User) (err error) {
    return
}

// 更新用户信息
func (l *user) Edit(ctx context.Context， req *domain.User) (err error) {
    return
}

// 删除指定用户
func (l *user) Delete(ctx context.Context， req *domain.IdPathReq) (err error) {
    return
}

// 分页查询用户
func (l *user) List(ctx context.Context， req *domain.UserListReq) (resp *domain.UserListResp， err error) {
    return
}

// 更新用户密码
func (l *user) UpdatePassword(ctx context.Context， req *domain.UpdatePasswordReq) (err error) {
    return
}
```

这里我们以创建用户和用户登陆接口为示例来讲解，企业接口类似。

1. 用户登陆

我们只需要在`internal/logic/user.go`文件中完善`Login`方法及，完善后的`Login`方法方法如下：

```go
// Login 用户登录验证
// 验证用户名和密码，成功后生成 JWT Token 并返回用户信息
func (l *user) Login(ctx context.Context， req *domain.LoginReq) (resp *domain.LoginResp， err error) {
    // 根据用户名查找用户
    user， err ：= l.svcCtx.UserModel.FindByName(ctx， req.Name)
    if err != nil {
       return nil， err
    }

    // 验证密码是否正确
    if !encrypt.ValidatePasswordHash(req.Password， user.Password) {
       return nil， errors.New("密码错误")
    }

    // 生成 JWT Token
    now ：= time.Now().Unix()
    token， err ：= token.GetJwtToken(l.svcCtx.Config.Jwt.Secret， now， l.svcCtx.Config.Jwt.Expire， user.ID.Hex())
    if err != nil {
       return nil， err
    }

    // 返回登录成功响应
    return &domain.LoginResp{
       Id：           user.ID.Hex()，                        // 用户ID
       Name：         user.Name，                            // 用户名
       AccessToken：  token，                                // JWT Token
       AccessExpire： l.svcCtx.Config.Jwt.Expire + now，     // Token 过期时间
    }， nil
}
```

- 新增用户

```go
// Create 创建新用户
// 检查用户名是否已存在，设置默认密码，加密后保存到数据库
func (l *user) Create(ctx context.Context， req *domain.User) (err error) {
    // 检查用户名是否已存在
    u， err ：= l.svcCtx.UserModel.FindByName(ctx， req.Name)
    if err != nil && err != model.ErrNotUser {
       return xerr.WithMessagef(err， "user model find by user err req.name %s"， req.Name)
    }
    if u != nil {
       return errors.New("已存在该用户")
    }

    // 设置用户密码，默认为 "123456"
    password ：= "123456"
    if len(req.Password) > 0 {
       password = req.Password
    }

    // 加密密码
    encryptPass， err ：= encrypt.GenPasswordHash([]byte(password))
    if err != nil {
       return xerr.WithMessagef(err， "encrypt.GenPasswordHash req.name %s"， password)
    }

    // 创建用户记录
    return l.svcCtx.UserModel.Insert(ctx， &model.User{
       Name：     req.Name，            // 用户名
       Password： string(encryptPass)， // 加密后的密码
    })
}
```

# 4. 功能测试

为了便于后续的编译，我们在项目根目录下创建一个启动脚本start.sh，脚本内容如下

```go
go mod tidy
go build -o AIWorkHelper
./AIWorkHelper
```

然后给脚本加上可执行权限

```go
chmod +x start.sh
```

接下来开始测试，在项目根目录下运行脚本

```go
./start.sh
```

可以看到程序正常启动了，在监听本地8888端口

![BlockNote image](https://ls8sck0zrg.feishu.cn/space/api/box/stream/download/asynccode/?code=NTM4ZDZmZTNlYmRiNmMwOGJlYWJlZDQ4ZjlhZWQwY2RfaFA2TTQwU1liRTFhZlR3WGlvTUY2SWVWVTR3VkJDRElfVG9rZW46QmVaZWJZWm93b1lHNnZ4ZHlhMmNrenFTbjdEXzE3Nzc4MzczNzM6MTc3Nzg0MDk3M19WNA)

然后我们就可以用apipost来测试登陆接口和创建用户接口了

## 4.1 登陆接口测试

我们用apipost来进行测试，打开apipost软件，新建一个AI办公助手的目录，后续用来测试这个项目下的所有接口

![BlockNote image](https://ls8sck0zrg.feishu.cn/space/api/box/stream/download/asynccode/?code=NzllYTMwYjY0ZWRlYTcyYzZmNzkzZmRjNmRjYzY1ZmJfSHg2elJuZG5nSDVjbkxYUElSWXRZUk9WM0F4a2FkYkpfVG9rZW46Q3FzRGJIamVtbzhPa2t4RWxYdWNENUdsbllmXzE3Nzc4MzczNzM6MTc3Nzg0MDk3M19WNA)

在`internal/handler/api/user.go`里面，goctl-gin工具已经为我们生成好了路由

```go
func (h *User) InitRegister(engine *gin.Engine) {
    g0 ：= engine.Group("v1/user")
    g0.POST("/login"， h.Login)

    g1 ：= engine.Group("v1/user"， h.svcCtx.Jwt.Handler)
    g1.GET("/：id"， h.Info)
    g1.POST(""， h.Create)
    g1.PUT(""， h.Edit)
    g1.DELETE("/：id"， h.Delete)
    g1.GET("/list"， h.List)
    g1.POST("/password"， h.UpdatePassword)
}
```

这里我们只需要根据这个路由信息在apipost创建用户登陆请求测试即可。在apipost我们刚刚穿件的AI办公助手的目录右侧点击“+”新建登陆接口。回顾我们的用户业务逻辑，在程序启动的时候，如果系统没有用户，那么系统会初始化创建一个系统用户，用户名为root，密码为123456。建立请求链接如下图所示

![BlockNote image](https://ls8sck0zrg.feishu.cn/space/api/box/stream/download/asynccode/?code=YzQ4MmU0MjIyZTdjNTk1YzA5OWFiYTVlZDc2ZDkzYTBfckM5OWgxMGpsVVVzR0xCR1pnejREREM3ZzRmNnJIQ0dfVG9rZW46UDVlUmJLa0dmb1NGNlN4TEttZGNRdkFqbjdjXzE3Nzc4MzczNzM6MTc3Nzg0MDk3M19WNA)

post请求，请求地址为：http：//localhost：8888/v1/user/login

请求参数这里选择raw类型，json格式

```json
{
    "name"： "root"，
    "password"： "123456"
}
```

发送之后可以得到如下返回，说明登陆接口是成功的

```json
{
    "code"： 200，
    "data"： {
        "id"： "689abec2f9e967e48510fe3f"，
        "name"： "root"，
        "token"： "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJleHAiOjE3NjM2MTE4NjEsImlhdCI6MTc1NDk3MTg2MSwiaW1vb2MuY29tIjoiNjg5YWJlYzJmOWU5NjdlNDg1MTBmZTNmIn0.VtJ2BPG9kepQyTmdvyoa59A1Zk-dFOXvoWIzWY2amrw"，
        "accessExpire"： 1763611861
    }，
    "msg"： "success"
}
```

![BlockNote image](https://ls8sck0zrg.feishu.cn/space/api/box/stream/download/asynccode/?code=OWMxZjM3YjU2NjQ2ZmY3OWQ3ZGM4NGYxMDI2YzRkMDlfYXZ1VmNPZ2laTXQ0SHNEMEFxWUFpN3lnOHNIMUF2T0pfVG9rZW46V3p0ZmIzRm5lb2tEMUF4a1hXamNJQWI1bkhiXzE3Nzc4MzczNzM6MTc3Nzg0MDk3M19WNA)

## 4.2 新增用户

由于新增用户是需要进行权限校验的，只有完成登陆的用户才有权限。所以我们需要先获取登陆token。执行以下命令获取token

```json
curl -X POST "http：//localhost：8888/v1/user/login" \
  -H "Content-Type： application/json" \
  -d '{
    "name"： "root"，
    "password"： "123456"
  }'
```

返回结果

```json
{"code"：200，"data"：{"id"："689abec2f9e967e48510fe3f"，"name"："root"，"token"："eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJleHAiOjE3NjM2MTI2MTMsImlhdCI6MTc1NDk3MjYxMywiaW1vb2MuY29tIjoiNjg5YWJlYzJmOWU5NjdlNDg1MTBmZTNmIn0.0A-To1KY5oCLGJ-YUFrjyJjN9vmENDamPQzYduq0qVE"，"accessExpire"：1763612613}，"msg"："success"}%
```

接下来就可以用apipost来测试了。同样的方法新建新增用户请求测试。

post请求，请求地址为：http：//localhost：8888/v1/user

注意由于新增用户需要token认证，所以要在apipost的认证栏架上Bearer Auth认证，value填写上面我们获取的token

![BlockNote image](https://ls8sck0zrg.feishu.cn/space/api/box/stream/download/asynccode/?code=YWMyMzE4Y2JkM2UxODg4YTQyYzA5Zjg4NDk3NTllNDdfOXBSbHowSnNWaFFnZ29BMVAyYm5FbmEzQ0VhR01FQUtfVG9rZW46QkI4SGJQRVV5b2lteGF4NG5HSGNpa0x2blJkXzE3Nzc4MzczNzM6MTc3Nzg0MDk3M19WNA)

然后再次选择Body，参数选择Raw类型，json格式，传递请求参数如下，新建一个用户zhangsan，密码为654321

```json
{
    "name"： "zhangsan"，
    "password"： "654321"
}
```

![BlockNote image](https://ls8sck0zrg.feishu.cn/space/api/box/stream/download/asynccode/?code=NjlkMzMyNjk5ZDMyZDRiMGI3MDQ4YWZmM2ZlZWI1NTFfcFMyaGVockk4SkZWeEg5ME5USEdqSTU0clhFbGJZZlFfVG9rZW46R2g1NmJmcHZnb1FCMmh4RWhWbGM5ZGhxbmw2XzE3Nzc4MzczNzM6MTc3Nzg0MDk3M19WNA)

点击发送之后，返回success

![BlockNote image](https://ls8sck0zrg.feishu.cn/space/api/box/stream/download/asynccode/?code=NmEyMzYxMTVhMTdjNzdhZDllZmU2OTkyZGRiMjRkNmNfSUhsQUVRSWJ3elRWbGNUSHN1TTA3eWV4WjJOZHM5RTNfVG9rZW46UVdhNWJjYVVNb3RWQm54Rlo1dWNkcWZSbmFjXzE3Nzc4MzczNzM6MTc3Nzg0MDk3M19WNA)

同时我们可以查看mongodb里的数据，点击刷新按钮后，用户zhangsan已经被成功添加，另一个则是系统启动时初始化的管理员用户root

![BlockNote image](https://ls8sck0zrg.feishu.cn/space/api/box/stream/download/asynccode/?code=NTViYWQ4MjRiOGY3NjMyYzU4MjE2MDMxNTNiY2I0ZDZfRzJDNEZPSldxM0FacktGU29oS2dXY3lBaHptQWtnUmdfVG9rZW46VTIzb2I4bVVvb3BEaTl4QTF2NmN0d3pEblhmXzE3Nzc4MzczNzM6MTc3Nzg0MDk3M19WNA)

以上是用apipost进行接口测试的详细教程。当然也可以用命令进行测试，更简单，比如通过以下命令同样可以测试新增用户接口

```sql
curl --request POST \
  --url http：//localhost：8888/v1/user \
  --header 'Authorization： Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJleHAiOjE3NjM2MTI2MTMsImlhdCI6MTc1NDk3MjYxMywiaW1vb2MuY29tIjoiNjg5YWJlYzJmOWU5NjdlNDg1MTBmZTNmIn0.0A-To1KY5oCLGJ-YUFrjyJjN9vmENDamPQzYduq0qVE' \
  --header 'content-type： application/json' \
  --data '{
    "name"： "zhangsan"，
    "password"： "654321"
}'
```

## 